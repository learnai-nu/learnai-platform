/opt/homebrew/Library/Homebrew/cmd/shellenv.sh: line 18: /bin/ps: Operation not permitted
---
interface Props {
	light?: boolean;
	compact?: boolean;
	locale?: 'da' | 'en';
}

const { light = false, compact = false, locale = 'da' } = Astro.props;
---

<a
	class:list={['orbit-brand', { 'orbit-brand-light': light, 'orbit-brand-compact': compact }]}
	href={locale === 'en' ? '/en' : '/'}
	aria-label={locale === 'en' ? 'LearnAI.nu – home' : 'LearnAI.nu – forside'}
>
	<svg class="orbit-brand-symbol" viewBox="0 0 44 44" aria-hidden="true">
		<rect width="44" height="44" rx="3"></rect>
		<path d="m10 15 7 7-7 7M21 29h13"></path>
	</svg>
	<span>LearnAI<span>.nu</span></span>
</a>
