---
import SiteLayout from '../../layouts/SiteLayout.astro';

const status = Astro.url.searchParams.get('status');
const statusMessages: Record<string, { text: string; error?: boolean }> = {
	success: { text: 'Tak! Du er på listen og får besked, når næste kursus åbner.' },
	invalid: { text: 'Kontrollér din e-mail og acceptér samtykket.', error: true },
	'save-error': { text: 'Tilmeldingen kunne ikke gemmes lige nu. Prøv igen om lidt.', error: true },
};
const statusMessage = statusMessages[status ?? ''];
---

<SiteLayout
	title="AI som dit daglige værktøj – 4 ugers praktisk forløb"
	description="Kom på ventelisten til LearnAI's fireugers forløb, og lær at spare 2–5 timer om ugen med AI på dine egne arbejdsopgaver."
>
	<section class="hero hero-compact level-two-hero">
		<div class="shell course-hero">
			<div>
				<p class="eyebrow">4 ugers hybridforløb · Kommer snart</p>
				<h1>AI som dit daglige værktøj.</h1>
				<p class="hero-copy">Gå fra AI-nysgerrig til AI-effektiv, og lær at spare 2–5 timer om ugen på rigtige arbejdsopgaver – på dansk og uden teknisk baggrund.</p>
			</div>
			<aside class="course-summary waitlist-summary">
				<strong>Få besked først</strong>
				<p>Fire praktiske uger, tre bonuslektioner og to live-workshops. Du får kursusplanen og introduktionsprisen, når forløbet åbner.</p>
				<a class="button" href="#venteliste">Kom på ventelisten</a>
			</aside>
		</div>
	</section>

	<section class="section section-tight">
		<div class="shell level-two-grid">
			<article class="level-two-card">
				<span>01</span>
				<h2>Kom i gang – og bliv sikker</h2>
				<p>Få en hurtig gevinst, forstå værktøjerne, beskyt data og lær de tre kontrolvaner.</p>
			</article>
			<article class="level-two-card">
				<span>02</span>
				<h2>Lær håndværket</h2>
				<p>Byg genbrugelige skabeloner, find din tone og forbedr svarene gennem korte iterationer.</p>
			</article>
			<article class="level-two-card">
				<span>03</span>
				<h2>Gør det permanent</h2>
				<p>Brug AI på dit rigtige arbejde, byg din digitale kollega, og dokumentér din tidsbesparelse.</p>
			</article>
		</div>
	</section>

	<section class="section waitlist-section" id="venteliste">
		<div class="shell waitlist-panel">
			<div>
				<p class="eyebrow">Venteliste</p>
				<h2>Tag det næste skridt med LearnAI.</h2>
				<p>Ingen spam. Du får kun relevante opdateringer om “AI som dit daglige værktøj” og kan altid afmelde dig igen.</p>
			</div>
			<div>
				{statusMessage && (
					<p class:list={['notice', { 'notice-error': statusMessage.error }]} role={statusMessage.error ? 'alert' : 'status'}>
						{statusMessage.text}
					</p>
				)}
				<form class="waitlist-form" method="post" action="/api/waitlist">
					<label>
						Fornavn <span>(valgfrit)</span>
						<input name="firstName" autocomplete="given-name" maxlength="80" />
					</label>
					<label>
						E-mail
						<input type="email" name="email" autocomplete="email" maxlength="254" required />
					</label>
					<label class="waitlist-consent">
						<input type="checkbox" name="consent" value="yes" required />
						<span>Jeg accepterer at modtage e-mails om kurset og LearnAI. Se vores <a href="/privatliv">privatlivspolitik</a>.</span>
					</label>
					<div class="honeypot" aria-hidden="true">
						<label for="company-website">Website</label>
						<input id="company-website" name="website" tabindex="-1" autocomplete="off" aria-hidden="true" />
					</div>
					<button class="button" type="submit">Giv mig besked</button>
				</form>
			</div>
		</div>
	</section>
</SiteLayout>
